Install for devlopper machine:
```shell
curl -sSf https://dotfiles.yejun.me/install.sh | sh -s -- dev
```
Install for server machine:
```
curl -sSf https://dotfiles.yejun.me/install.sh | sh -s -- server
```
